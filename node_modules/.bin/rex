#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../rex-cli/index.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../rex-cli/index.js" "$@"
  ret=$?
fi
exit $ret
