#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../network-address/cli.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../network-address/cli.js" "$@"
  ret=$?
fi
exit $ret
